This change was made in `repos/radio_ocean` (its code is the Python simulations under
`notes/scenes/ari-roping/`, its export lands in this repository's `data/`) and archived here,
in the game's repository, on 2026-09-24 at the author's request, so that all the roping work has
one record. Paths in these artifacts are relative to `radio_ocean` unless they name `repos/ari-roping`.
